class Solution {
public:
    vector<int> findArray(vector<int>& pref) {
        int size = pref.size();

        for (int i = size - 1; i > 0; i--) {
            pref[i] = pref[i] ^ pref[i - 1];
        }

        return pref;
    }
};
